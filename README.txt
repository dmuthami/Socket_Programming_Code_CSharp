The Code repository below was developed using C# as a socket server application. The application is meant to listen asynchronously for clients which for this 
case was tracking gadgets fitted into trucks polling every minute. The socket server application was to listen on incomming traffic, grab the data and store it into a PostgreSQL database.

The application is based on socket programmming concepts for .NET and specifically utlizing C#.

